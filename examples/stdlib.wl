avg arg : inType -> outType
  total = 0
  count = 0
  foreach i in [1, 2, 3]
    total = total + i
    count = count + 1
  average = (total / count)
  log "done"

//gcd arg : inType -> outType
  //strlen "hey"
  //log "heyee"
